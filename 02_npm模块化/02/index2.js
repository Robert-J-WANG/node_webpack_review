/**
 * 目标：基于 ECMAScript 标准语法，"默认"导入，工具属性和方法使用
 */
// 分别导入
import { baseURL, getArraySum } from "./utils2.js";
// 使用
console.log(baseURL);
console.log(getArraySum([1, 2, 3, 4, 5, 6, 7, 8, 9]));
